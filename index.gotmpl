<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
        rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
        crossorigin="anonymous">
    <script src="https://unpkg.com/htmx.org@1.9.10"
        integrity="sha384-D1Kt99CQMDuVetoL1lrYwg5t+9QdHe7NLX/SoJYkXDFfX37iInKRy5xLSi8nO7UC"
        crossorigin="anonymous"></script>
    <title>htmx + Go app</title>
</head>
<body>
    <div class="row m-4 g-4">
        <div class="col-8">
            <h1 class="mb-4">Film List</h1>

            <ul id="film-list" class="list-group fs-5 me-5">
                {{ range .Products }}
                    {{ block "film-list-element" . }}
                    <li class="list-group-item {{ if .Available }}bg-primary{{ else }}bg-danger{{ end }} text-white ">
                            {{ .Name }}: ${{ .Price }}
                        </li>
                    {{ end }}
                {{ end }}
            </ul>
        </div>

        <div class="col-4">
            <h1 class="mb-4">Add Product</h1>

            <form hx-post="/add-product/"
                hx-target="#film-list"
                hx-swap="beforeend"
                hx-indicator="#spinner">
                <div class="mb-4">
                    <label for="product-name">Name</label>
                    <input type="text" name="name" id="product-name" class="form-control" />
                </div>
                <div class="mb-3">
                    <label for="product-price">Price</label>
                    <input type="number" name="price" id="product-price" min="0" step="0.01" class="form-control" />
                </div>
                <div class="mb-3">
                    <label for="product-available">Product is available:</label>
                    <input type="checkbox" name="available" id="product-available" class="" checked /> <!-- Add a bootstrap class? -->
                </div>

                <button type="submit" class="btn btn-primary">
                    <span class="spinner-border spinner-border-sm htmx-indicator" id="spinner" role="status" aria-hidden="true"></span>
                    Submit
                </button>
            </form>
        </div>
        <button class="btn btn-primary col-2"
            hx-post="/load-dummy-data/"
            hx-target="#film-list"
            hx-swap="beforeend">
            Load dummy data
        </button>
    </div>
</body>
</html>
